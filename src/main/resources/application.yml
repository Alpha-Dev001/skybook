server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/skybook_db
    username: postgres
    password: mu1ne2ze3ro4

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  application:
    name: SkyBook

logging:
  level:
    org.hibernate.SQL: DEBUG